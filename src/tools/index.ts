/**
 * Tool registry (PRD §6.3) — lands in M2.
 * Tools defined once in server/tools/, schema inferred from TypeScript,
 * powering function calling, MCP, A2A, and agent-to-agent calls.
 */
export const MODULE_STATUS = 'planned:M2' as const;
